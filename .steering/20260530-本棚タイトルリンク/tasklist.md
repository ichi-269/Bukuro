# タスクリスト: 本棚タイトルリンク（記事確認）

## 申し送り

### 実装完了日
2026-05-30

### 計画と実績の差分
- 差分なし。タスクリスト通りに実装完了。

### 学んだこと
- Thymeleaf の `th:if` / `th:unless` を対称ペアで使うと、`th:else` 相当の排他的分岐を1 `<h6>` 内に簡潔に表現できる。
- フラグメントパラメータとして渡されていないモデル変数（例: `postIdByBookId`）も、同一ページのフラグメントからはスコープ上アクセス可能。

### 次回への改善提案
- `ShelfController` のテストが存在しないため、`postIdByBookId` の構築ロジック（複数記事/同じ本に複数記事がある場合の重複解決）をカバーするユニットテストを追加するとよい。

- [x] shelf/index.html: 書籍カードのタイトルを条件付きリンクに変更する
